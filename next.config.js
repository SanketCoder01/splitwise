/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'api.resuchain.gov.in', 'images.unsplash.com', 'upload.wikimedia.org'],
  },
}

module.exports = nextConfig
